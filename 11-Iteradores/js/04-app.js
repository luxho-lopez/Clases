let i = 1;

while (i <= 10) { // While loop se ejecuta mientras la condición sea verdadera
    if(i % 2 === 0) {
        console.log(`${i} es par`);
    } else {
        console.log(`${i} es impar`);
    }
    i++;
}